# Example env file for backend. Copy to `.env` and fill values.
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_db_password_here
DB_NAME=edusync_db
PORT=3000

# Optional: PGAdmin credentials
PGADMIN_EMAIL=admin@your-domain.local
PGADMIN_PASSWORD=your_pgadmin_password